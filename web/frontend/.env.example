# Base URL of the FastAPI backend exposed by web/backend.
# Override this when serving the backend from a different host/port,
# e.g. behind an Nginx proxy or a docker-compose service name.
VITE_API_BASE_URL=http://localhost:8000

# Local dev-server port; Vite will pick the next free port if taken.
VITE_DEV_PORT=5173
