name: token-heatmap
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  # Core ML stack — installs CUDA-enabled PyTorch when the pytorch+nvidia channels are used.
  # For CPU-only: replace with `pytorch::pytorch>=2.1 cpuonly`
  - pytorch::pytorch>=2.1
  - pytorch::torchvision>=0.16
  - conda-forge:pandas>=2.0
  - conda-forge:numpy>=1.24
  - conda-forge:matplotlib>=3.8
  - conda-forge:pyyaml>=5.1
  - conda-forge:pip
  - pip:
    # transformers/accelerate lag slightly behind conda-forge, so install via pip
    - transformers>=5.0
    - accelerate>=0.30
    # Qwen2 tokenizer extras
    - tiktoken>=0.6
    - einops>=0.7
    # Backend (FastAPI server + activation-diff endpoint)
    - fastapi>=0.110
    - "uvicorn[standard]>=0.27"
    - pydantic>=2.5
    - pydantic-settings>=2.1
    - python-multipart>=0.0.6
    # Dev / test
    - pytest>=7.0
    - pytest-cov>=4.0
    - httpx>=0.26
    - jsonschema>=4.20
    # Install the library and backend in editable mode
    - "-e ."
    - "-e web/backend/"
